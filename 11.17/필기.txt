UI구성요소   JFrame[하나의 창 전부?] JBotton[버튼들] JLabel[글자나 그림을 나타내는 컴포넌트]
총칭해서 컴포넌트라고함
폰트 컬러는 컴포넌트아님 성질 컴포넌트는 구성요소

어댑터 제공자가 제공하는 어떤 메소드들이 사용자와 호환되지않을 때 어댑터를 사용한다

EX3 다른객체[B->A]에 다이렉트로 접근하지말고 멤버변수 건들 때 B에서 메소드를 만들어서 
a를 건들기 [plusOrMinus 호출해서 변경해라]

ex4 똑같은데 버튼이 하나 늘어남 doAction 가지고 있고 B는 doAction이용해서 변경

class Some -a +using A +change A 
Some은 a를 이용하거나 바꾸는 메소드를 가졌음 이런 메소드를 가진 의미는
some 제공하는 연산만 허가한다는 의미 [a를 직접바꾸지마라]

클래스 연결 : 서로 정보를 호출 공유 연산 하기 위해 

use-a관계일 때 서로서로 사용을 할 때 메소드를 사용하는것이 가장 좋다

ex3 생성자를 통해 서로 연결함


ex5         3~4는 아우터를 위해서만 존재하는데 왜 따로 뺏냐?
처리할 수 있는 방법 이너클래스 혹은 범용성지닌 클래스 만들어 사용하는 것
1번 이너클래스 아우터만 위해 존재하면 이너클래스로 넣을 것 고려 선택? 따로 뗄 필요없음
2번 따로 뗄 필요가 있어보인다면 범용성지닌 클래스 스태틱클래스 만들어 사용하는것 고려?
밖에서 따로 떼서 존재하는 클래스가 다른곳에서 사용되지 않는다면 의미가 없다 
그런 상황을 만들어선 안됨


6번 한번 사용하게 된다면 이름없는 anoymous 이너 클래스를 사용 할 수도 있다.
